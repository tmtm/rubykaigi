- @title = t(:gold_sponsor)
#content
  %h1
    #{t(:gold_sponsor)}

  - RubyKaigi2011::GoldSponsor.all.each do |sponsor|
    %article
      %h2
        = sponsor.localize(I18n.locale, :name)
      %a{:name => sponsor._id}
        %img.logo{:alt => "Example Company, Ltd.", :src => "/2011/sponsors/#{sponsor._id}.png"}
      %div
        %div
          %p
            = sponsor.localize(I18n.locale, :pr)
          %p.right
            %a.linkMark{:href => "#{sponsor.url}", :target => "_blank"}
              = sponsor.url
    != render :partial => 'shared/2011/back_to_top'
