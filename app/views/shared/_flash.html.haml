#flash{:style => 'display:none'}
  %div{:class => level}
    %span #{flash_message}

:javascript
  function fout() {
    $('#flash').fadeOut('slow');
  }
  $(document).ready(function() {
    $('#flash').fadeIn('slow');
    setTimeout('fout()', 3000);
  });
